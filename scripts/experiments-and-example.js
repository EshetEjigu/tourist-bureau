"use strict"



const initial = 0
const step = 1
const final = colors.length - 1




let currentIndex = initial  // defining
while (currentIndex <= final) { // checking
    console.log(`color number ${currentIndex} is ${colors[currentIndex]}!`)
    
    
    currentIndex += step   // changing
    
}
const colors = ["periwinkle", "sand brown", "salmon", "seagreen", "midnight blue"]


// defining         // checking        // changing 
for (let index = 0; index < colors.length; index+= 1) {
    console.log(`color number ${index} is ${currentcolor}!`)  
}
    
    
for (let index = 0; index < colors.length ; index ++ ) {
    console.log(currentColor)
console.log(`color number ${index} is ${colors[index]}!`)  
}


let x = x + 1  // Does two things: addition & assignment
x += 1   // Does two things: addition & assignment
++x   //Dose two things: addition & assignment
x++  //Dose three: addition return the OLD CSSMathValue,assignment 

console.log("when dose this console.log run???")